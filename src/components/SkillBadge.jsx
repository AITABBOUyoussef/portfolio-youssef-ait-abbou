function SkillBadge({ label }) {
  return (
    <span className="inline-block rounded-full border border-slate-700 bg-slate-800/50 px-3 py-1 text-xs text-slate-200">
      {label}
    </span>
  );
}

export default SkillBadge;
